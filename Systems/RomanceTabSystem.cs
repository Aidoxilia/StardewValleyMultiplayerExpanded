namespace PlayerRomance.Systems;

// Kept as a stub so ModEntry property and instantiation compile.
// All Romance tab rendering and click-handling is now done via
// Patches/GameMenuPatches.cs (Harmony postfix/prefix on GameMenu).
public sealed class RomanceTabSystem
{
    public RomanceTabSystem(ModEntry mod) { }
}

